(define (cddr s)
  (cdr (cdr s)))

(define (cadr s)
  (car (cdr s))
)

(define (caddr s)
  (car (cddr s))
)


(define (sign num)
  'YOUR-CODE-HERE
)


(define (square x) (* x x))

(define (pow x y)
  'YOUR-CODE-HERE
)

